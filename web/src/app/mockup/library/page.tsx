import { LibraryPage } from "@/components/mockup/library-page";

export const metadata = { title: "Materials Library — Mockup" };

export default function MockupLibraryPage() {
  return <LibraryPage />;
}
