import { CanvasWorkspace } from "@/components/mockup/canvas-workspace";

export const metadata = {
  title: "Mockup — Canvas + Inspector",
};

export default function MockupPage() {
  return <CanvasWorkspace />;
}
