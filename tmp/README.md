Temporary working files live here.

Keep generated JSON, CSV, workbook exports, and fetch logs in `tmp/`, but do not commit them.
The repository ignores everything in this folder except this short note so the working area stays available without polluting Git history.
